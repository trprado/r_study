# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g. crates.io) dependencies
#
# If you believe there's an error in this file please file an
# issue against the rust-lang/cargo repository. If you're
# editing this file be aware that the upstream Cargo.toml
# will likely look very different (and much more reasonable)

[package]
name = "rgb"
version = "0.8.11"
authors = ["Kornel Lesiński <kornel@geekhood.net>"]
include = ["src/**/*", "Cargo.toml", "README.md", "examples/*.rs"]
description = "`struct RGB` (etc.) for sharing pixels between crates.\nAllows no-copy high-level interoperability. Also adds common convenience methods and implements standard Rust traits to make RGB/RGBA pixels first-class Rust objects."
homepage = "https://crates.rs/crates/rgb"
documentation = "https://kornelski.github.io/rust-rgb/rgb/"
readme = "README.md"
keywords = ["rgb", "rgba", "bgra", "pixel", "interoperability"]
categories = ["rust-patterns", "multimedia::images"]
license = "MIT"
repository = "https://github.com/kornelski/rust-rgb"

[[example]]
name = "serde"
required-features = ["serde"]

[[example]]
name = "example"
[dependencies.serde]
version = "1.0"
features = ["derive"]
optional = true
[dev-dependencies.serde_json]
version = "1.0"
[badges.travis-ci]
repository = "kornelski/rust-rgb"
